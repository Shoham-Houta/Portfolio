# Wrangler serves the repo root as static assets, so anything not excluded here
# is publicly reachable at houta.dev/<path>. Keep repository plumbing out.
.git
.git/**
.gitignore
.assetsignore
.wrangler
.wrangler/**
node_modules
node_modules/**
README.md
*.md
.DS_Store

# Source for assets/og.png — regenerated, never served.
tools
tools/**
